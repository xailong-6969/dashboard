{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate --schema prisma/schema.prisma && cd apps/web && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --schema prisma/schema.prisma --accept-data-loss && cd apps/web && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}
