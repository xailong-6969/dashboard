{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/web && npm install && npx prisma generate --schema ../../prisma/schema.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/web && npx prisma db push --schema ../../prisma/schema.prisma --accept-data-loss && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}
