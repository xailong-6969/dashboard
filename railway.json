{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate && next build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss && next start --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
